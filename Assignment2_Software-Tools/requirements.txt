# =============================================================================
# Predicting Airbnb Listing Prices with MLflow and AWS S3
# AML-3303 Assignment 2 — StayWise Data Science Team
#
# Install all dependencies:
#   pip install -r requirements.txt
#
# Python version: 3.10+
# =============================================================================

# ── AWS / Cloud ───────────────────────────────────────────────────────────────
boto3>=1.42.81           # AWS SDK — stream objects directly from S3
botocore>=1.42.81        # Low-level AWS client (boto3 dependency, unsigned config)

# ── Data manipulation ─────────────────────────────────────────────────────────
pandas>=2.3.3            # DataFrame loading, cleaning, feature engineering
numpy>=2.3.5             # Numerical operations, log/exp transforms, percentiles

# ── Visualisation ─────────────────────────────────────────────────────────────
matplotlib>=3.10.8       # Base plotting (histograms, bar charts, scatter plots)
seaborn>=0.13.2          # Statistical visualisations (heatmaps, distribution plots)

# ── Machine learning ──────────────────────────────────────────────────────────
scikit-learn>=1.8.0      # Models (RF, GBM, Ridge, Lasso, LinearRegression),
                         # preprocessing (StandardScaler), evaluation metrics,
                         # model selection (KFold, cross_val_score, ParameterGrid)

# ── Experiment tracking & model registry ─────────────────────────────────────
mlflow>=3.10.1           # Experiment logging, artifact storage, Model Registry
